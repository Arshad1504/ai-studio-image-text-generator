{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/$1"
        }
    ],
    "functions": {
        "api/*.js": {
            "maxDuration": 60
        }
    },
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "framework": "vite"
}